---
interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props;
const headingId = `${id}-heading`;
---

<section class="page-section" id={id} aria-labelledby={headingId}>
  <h2 id={headingId}>{title}</h2>
  <slot />
</section>
