---
interface Props {
  class?: string;
  size?: 'full' | 'editorial' | 'wide' | 'reading';
}

const { class: className, size = 'full' } = Astro.props;
---

<div class:list={['container', `container--${size}`, className]}>
  <slot />
</div>
