---
interface Props {
  href: string;
  variant?: 'primary' | 'secondary';
  ariaLabel?: string;
}

const { href, variant = 'primary', ariaLabel } = Astro.props;
---

<a class:list={['button', `button--${variant}`]} href={href} aria-label={ariaLabel}>
  <slot />
</a>
